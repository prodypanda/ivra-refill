## 2026-06-06 - Add Tooltips to IconButtons
**Learning:** In Flutter, icon-only buttons (like `IconButton`) lack semantic labels by default, making them inaccessible to screen readers and difficult to understand without context.
**Action:** Always provide a `tooltip` property containing a localized string for `IconButton` and other icon-only interactive widgets to ensure accessibility and provide semantic labels for screen readers.
