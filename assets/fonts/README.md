# PDF Report Fonts

Ivra PDF exports use DejaVu Sans regular and bold fonts so generated reports can render Unicode text without falling back to PDF base fonts.

The font files are bundled as app assets:

- `DejaVuSans.ttf`
- `DejaVuSans-Bold.ttf`

These assets are used only by report generation.
